<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Requested_Shared_RM__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Proposed Shared RM for a Share Deal approval request.</description>
    <label>Requested Shared RM</label>
    <!-- No lookup filter: Profile.Name filters fail for Sales Admin (no Profile visibility)
         and surface as "Value does not exist or does not match filter criteria".
         Apex ShareDealApprovalService still enforces Sales Manager MIRA. -->
    <referenceTo>User</referenceTo>
    <relationshipLabel>Share Deal Approval Requests</relationshipLabel>
    <relationshipName>Share_Deal_Approval_Requests</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
